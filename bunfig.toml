# Register happy-dom globally so component tests (React Testing Library) have a DOM.
# Harmless for server tests, which simply don't touch the DOM globals.
[test]
preload = ["./apps/client/happydom.ts"]
